<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TaskDudes.Views.LoginPage"
             Shell.NavBarIsVisible="False">
    <ContentPage.Content>
        <Grid BackgroundColor="#1d3b6b">
            <StackLayout Padding="10,0,10,0" Spacing="20"  VerticalOptions="Center">
                <Entry Text="{Binding Username}" Placeholder="User name"></Entry>
                <Entry Text="{Binding Password}" Placeholder="Password" IsPassword="True"></Entry>
                <Button VerticalOptions="Center" Text="Login" Command="{Binding LoginCommand}"/>
                <Label VerticalOptions="Center" Text="No account yet? Register here" TextColor="blue">
                    <Label.GestureRecognizers>
                        <TapGestureRecognizer Command="{Binding RegisterCommand}" />
                    </Label.GestureRecognizers>
                </Label>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>