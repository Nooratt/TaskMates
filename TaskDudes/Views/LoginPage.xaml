<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TaskDudes.Views.LoginPage"
             Shell.NavBarIsVisible="False">
    <ContentPage.Content>
        <Grid BackgroundColor="#1d3b6b">
            <StackLayout Padding="10,0,10,0" Spacing="20"  VerticalOptions="Center">
                <Entry x:Name="username" Text="{Binding Username}" Placeholder="User name" TextColor="white" PlaceholderColor="Gray"></Entry>
                <Entry x:Name="password" Text="{Binding Password}" Placeholder="Password" IsPassword="True" TextColor="white" PlaceholderColor="Gray"></Entry>
                <Button VerticalOptions="Center" Text="Login" Command="{Binding LoginCommand}"/>
                <Button VerticalOptions="Center" Text="Register" Command="{Binding RegisterCommand}"/>
            </StackLayout>
        </Grid>
    </ContentPage.Content>
</ContentPage>